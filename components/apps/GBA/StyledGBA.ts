import styled from "styled-components";

const StyledGBA = styled.div`
  canvas {
    background-color: #000;
  }

  div {
    font-family: Liberation Mono, DejaVu Sans Mono, Courier New, monospace;
    font-size: 14px;
    font-weight: bold;
    line-height: normal;
    position: relative;
    white-space: pre;
  }
`;

export default StyledGBA;
