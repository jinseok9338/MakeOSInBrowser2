export type colors = {
  primary: string;
  secondary: string;
};

export type colorsOptions = {
  primary?: string;
  secondary?: string;
};

export const colors = {
  primary: "#FFF",
  secondary: "#000",
};
