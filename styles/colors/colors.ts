export type colors = {
  primary: string;
  secondary: string;
};

export type colorsOptions = {
  primary?: string;
  secondary?: string;
};

export const colors = {
  primary: "#FFAEBC",
  secondary: "#A0E7E5",
};
