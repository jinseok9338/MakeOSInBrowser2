export type sizes = {
  taskbar: {
    width: string;
    height: string;
  };
};

export type sizesOptions = {
  taskbar?: {
    width?: string;
    height?: string;
  };
};

export const sizes = {
  taskbar: {
    width: "100%",
    height: "30px",
  },
};
