export type sizes = {
  taskbar: {
    width: string;
    height: string;
  };
};

export type sizesOptions = {
  taskbar?: {
    width?: string;
    height?: string;
  };
};

export const sizes = {
  taskbar: {
    width: "10px",
    height: "10px",
  },
};
